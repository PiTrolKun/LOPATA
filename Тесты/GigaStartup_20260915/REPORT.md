# Сбой проверки Giga — 15 сентября 2026

Скриншот пользователя: `Giga worker exited -1073740791` при подготовке литературного сценария 0.1.109-dev.

Windows Application 1000, 04:36:47: python.exe из изолированного Giga-runtime, PID 0xC42C, модуль torch_cpu.dll, exception 0xc0000409, offset 0xcd2893d. Это соответствует показанному exit code. Сохранены событие и журнал worker (stderr отсутствует, в журнале только статистика ресурсов). Первопричина по событию не установлена; наличие torch_cpu.dll в событии не доказывает повреждение установки или нехватку VRAM.

Тот же python.exe и поставляемый Tools/giga_embeddings.py --check запущены отдельно четыре раза, каждый раз новый процесс, с -X faulthandler. Все четыре вернули `ready:true, cuda:true`, exit 0. Это проверка импорта/доступности CUDA, не полноценная индексация книги. Сбой не воспроизведён. Версии зависимостей, защита Windows, модель и код приложения не менялись; автоматического перезапуска или обхода проверки не добавлено. Рекомендуемый следующий пользовательский шаг — «Проверить снова». При повторении нужна диагностика нативного падения, а не переустановка на основании единственного события.
