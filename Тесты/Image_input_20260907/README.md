# Проверка общего ввода изображений — 2026-09-07

## Приёмка — 2026-09-07

Пользователь подтвердил работу окончательного варианта: «Теперь все ок. ТЗ выполнен». [ТЗ закрыто](../../ТЗ/Архив/2026-09-07_Общий_ввод_и_хранение_изображений.md), версия 0.1.52-dev. Ниже сохранены результаты проверок и замечания отдельных этапов; ожидание приёмки в старых записях больше не является текущим статусом. Установщик не пересобирался.

## 2026-09-07 — исправлена вставка изображений, 0.1.52-dev

Пользователь подтвердил корректное перетаскивание и использование локальных файлов без копирования. По его уточнению убрана отдельная кнопка вставки. Ctrl+V перенесён с UserControl на PreviewKeyDown главного активного окна и разрешён только при видимой странице анализа на шаге «Изображение»; фокус на карточке не требуется. Текстовые поля сохраняют стандартную вставку, занятые/завершённые/read-only/ContextBlocked работы защищены. Drag/drop не менялся.

Собственное ImageRetentionWindow удалено: оно давало нечитаемый текст в реальной теме пользователя. После получения и проверки временной копии пикселей/URL показывается нативный Windows MessageBox YesNoCancel: Да — оставить, Нет — удалить после работы, Отмена — отменить импорт. Переданный существующий локальный файл не копируется, вопрос не появляется. Поиск совпадающей картинки по диску не выполняется; политика определяется переданным источником.

Проверки: 414/414 .NET; 230 направленных проверок стенда `_tmp/image_paste_check` (три режима, RU/EN, обе темы, фокус вне карточки, текстовые поля, шаги/состояния, DataObject). В реальном системном диалоге проверены полный текст, стандартные Yes/No/Cancel и результат отмены; буфер пользователя не менялся. Restore/build 0 ошибок и предупреждений, safe-start выполнен. ТЗ и история уточнены; ТЗ остаётся активным. Установщик не пересобирался, commit/push не выполнялись. Backup до исправления: `_backups/image_paste_correction_20260907/files.txt`.

Автоматические проверки не заменяют пользовательское нажатие Ctrl+V с содержимым его буфера в основной программе. Системный MessageBox проверялся в отдельном тестовом процессе; его собственные окна закрывались через Cancel, пользовательские окна не затрагивались.

Версия 0.1.51-dev. Реализация готова к пользовательской проверке; ТЗ активно.

## Что проверено

- `dotnet restore` и build: успешно, 0 предупреждений/ошибок.
- `dotnet test Исходники/AIHub.Tests/AIHub.Tests.csproj --no-restore`: 414/414. Добавлены 15 проверок: приём пикселей/URL/файла, владение и очистка, постоянные дубли, защита другого экземпляра, остатки аварии, чужой путь, HTML вместо JPEG, превышение размера с известной и неизвестной длиной, редиректы/цикл/запрещённая схема, отмена/ошибка таймаута, текстовый backup без скрытой картинки, старые паспорта.
- Изолированный WPF/процессный стенд `_tmp/image_input_check`: 159 проверок. DataObject FileDrop/PNG/Bitmap/прямая ссылка/HTML img; отказ нескольких файлов/картинок; текстовые поля не перехватываются. Альфа/Бета/Гамма, RU/EN, обе темы: доступность вставки, запреты при занятости/read-only/completed/ContextBlocked, путь в tooltip, пропавший исходник, доступность просмотра/экспорта текста. Переключение стандартного промпта не обходит запрет анализа отсутствующей картинки.
- Стенд создал отдельный процесс с открытой эксклюзивной арендой файла: очистка живого владельца файл сохранила; после принудительного завершения именно этого тестового процесса следующий экземпляр файл удалил. Принудительно основная программа и пользовательские процессы не завершались. Запущенная агентом dev-копия штатно закрывалась для завершающей пересборки и запущена снова.
- Реальный GET ссылки `https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSfvQ6omtjvpifVZEqbLcZ51cGejG8pnpP7xF831GTVBA&s=10`: JPEG, 563×545, 28104 байта. Расширение URL не требуется; определён формат содержимого.
- Рендеры пустой карточки и окна выбора хранения просмотрены; путь к ним `_tmp/image_input_check`. Буфер обмена пользователя не менялся: автоматические проверки использовали DataObject.
- Проверка UTF-8/кириллицы затронутых исходников и документов, паритет ключей RU/EN и `git diff --check`: успешно.
- Штатный `start-aihub.ps1`: сборка и запуск успешны, окно «ЛОПАТА 0.1.51-dev», процесс отвечает.

## Границы подтверждения

Это проверки механизма ввода/хранения и UI. Новый полный анализ моделями не запускался; модели, sampling, prompts, голос и загрузчик моделей не менялись. Фактические жесты Copy Image/drag-and-drop в конкретных браузерах пользователя ещё требуют его проверки: проверены их форматы передачи и реальный сетевой URL. Таймаут проверен через исключение транспорта, длительного ожидания сетевого зависания не было. Отключение питания не имитировалось, проверен принудительно завершённый процесс.

Временные файлы после аварии удаляются при следующем запуске, при блокировке файла ОС очистка повторяется позднее. Постоянные копии и пользовательские исходники не удаляются. Картинки, которым пользователь выбрал «Оставить», остаются в общей папке даже при последующей ошибке записи работы. Системные форматы изображений зависят от установленных WIC-кодеков; страницы HTML не загружаются как изображения.

Установщик не пересобирался по правилу. Commit/push/архивация не выполнялись до приёмки. Чужое удаление файла идеи и остальные личные файлы не изменялись.
